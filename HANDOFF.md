# Handoff

This file will be generated from machine state once the runtime exists. Until then,
`CONTINUITY.md`, `mission/contract.json`, the Git history, and the current iteration hypothesis are
the authoritative recovery context.

